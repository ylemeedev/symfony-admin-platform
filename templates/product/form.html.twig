{{ form_start(form, { method: 'POST', attr: { class: 'space-y-6' } }) }}

<div>
	{{ form_label(form.name, 'Nom', {
			label_attr: {
				class: 'block text-sm font-medium text-gray-700 mb-1'
			}
		}) }}

	{{ form_widget(form.name, {
			attr: {
				class: 'w-full px-3 py-2 rounded-lg border border-gray-300 focus:border-indigo-500 focus:ring-indigo-500'
			}
		}) }}

	{{ form_errors(form.name, {
			attr: { class: 'text-sm text-red-600 mt-1' }
		}) }}
</div>

<div>
	{{ form_label(form.reference, 'Référence', {
			label_attr: {
				class: 'block text-sm font-medium text-gray-700 mb-1'
			}
		}) }}

	{{ form_widget(form.reference, {
			attr: {
				class: 'w-full px-3 py-2 rounded-lg border border-gray-300 focus:border-indigo-500 focus:ring-indigo-500'
			}
		}) }}

	{{ form_errors(form.reference, {
			attr: { class: 'text-sm text-red-600 mt-1' }
		}) }}
</div>

<div>
	{{ form_label(form.description, 'Description', {
			label_attr: {
				class: 'block text-sm font-medium text-gray-700 mb-1'
			}
		}) }}

	{{ form_widget(form.description, {
			attr: {
				class: 'w-full px-3 py-2 rounded-lg border border-gray-300 focus:border-indigo-500 focus:ring-indigo-500',
				rows: 4
			}
		}) }}

	{{ form_errors(form.description, {
			attr: { class: 'text-sm text-red-600 mt-1' }
		}) }}
</div>

<div>
	{{ form_label(form.price, 'Prix', {
			label_attr: {
				class: 'block text-sm font-medium text-gray-700 mb-1'
			}
		}) }}

	{{ form_widget(form.price, {
			attr: {
				class: 'w-full px-3 py-2 rounded-lg border border-gray-300 focus:border-indigo-500 focus:ring-indigo-500'
			}
		}) }}

	{{ form_errors(form.price, {
			attr: { class: 'text-sm text-red-600 mt-1' }
		}) }}
</div>

<div>
	{{ form_label(form.minimumStock, 'Stock minimum', {
			label_attr: {
				class: 'block text-sm font-medium text-gray-700 mb-1'
			}
		}) }}

	{{ form_widget(form.minimumStock, {
			attr: {
				class: 'w-full px-3 py-2 rounded-lg border border-gray-300 focus:border-indigo-500 focus:ring-indigo-500'
			}
		}) }}

	{{ form_errors(form.minimumStock, {
			attr: { class: 'text-sm text-red-600 mt-1' }
		}) }}
</div>

<div class="pt-4">
	{{ form_widget(form.save, {
			attr: {
				class: 'w-full bg-indigo-600 text-white font-semibold py-2 px-4 rounded-lg hover:bg-indigo-700 transition'
			}
		}) }}
</div>

{{ form_rest(form) }}
{{ form_end(form) }}
